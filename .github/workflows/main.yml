on:
  push:
    branches:
      - master

name: actions test

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: log json
        run: echo ${{ secrets.TEST_JSON }}
      
      - name: docker test
        run: docker